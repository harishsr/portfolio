.blog
  .blog-title.wide
    %h1.display-3
      Harish's Notebook
    - if admin_signed_in?
      = link_to "New Entry", new_entry_url, class: 'btn btn-success'

- entries.each do |ent|
  %h3= link_to ent.title, ent

  - if ent.image.present?
    = image_tag ent.image.url(:medium)

  %p= simple_format(ent.content)
