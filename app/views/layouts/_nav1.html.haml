.row
  .col-md-4
    = link_to "Blog", root_url

  .col-md-4
  .col-md-4
    - if admin_signed_in?
      Welcome, 
      = current_admin.username
      = link_to "Sign off", destroy_admin_session_url, title: "Sign off", method: :delete
    
    - else
      = link_to "Sign in", new_admin_session_url, title: "Sign in"
